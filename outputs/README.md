# Outputs Folder

Generated files belong here:

- `obj/` — raw and cleaned OBJ files.
- `stl/` — print-focused STL files.
- `previews/` — PNG renders/contact sheets.
- `manifests/` — JSON manifests.

Keep raw exports. Mesh repair is an edit, not an erasure.
