# Data Folder

This folder holds intermediate forms of memory:

- `features/` — CSV feature tables.
- `fields/` — NumPy scalar fields or other volume data.
- `manifests/` — additional manifest copies or indexes.

Generated data is usually gitignored unless it is part of a documented example.
